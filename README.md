# Accident report class showing totals for the accidents in each city.
It features overloading, using private fields in the class and uses public getters and setters to access and change the values. It also features dot notation to set values to empty parameters in the objects
